[build]
builder = "nixpacks"
buildCommand = "CI=false npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/app.js"
restartPolicyType = "on_failure"
